# jazzaidashboardgenerator

Next.js frontend for the AI Dashboard Generator.

## Local Development

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` in `.env.local` to point at the FastAPI backend.
